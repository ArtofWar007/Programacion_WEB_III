// ¿Cuando es conveniente utilizar un callback, y cuando es necesario utilizar una
// promesa?
const pregunta = "¿Cuando es conveniente utilizar un callback, y cuando es necesario utilizar una promesa?\n";

const respuesta = "RESPUESTA: En el desarrollo actual no existe ninguna convenniencia tecnica para usar callback en tareas asincronas, pero aun siguen siendo mejor opción para eventos recurrentes como un boton o en iteradores de arreglos como .map() o .filter(), no hay asincronia.";

console.log(pregunta);
console.log(respuesta);